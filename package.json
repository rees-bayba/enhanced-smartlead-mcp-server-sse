{
  "name": "enhanced-smartlead-mcp-server-sse",
  "version": "1.1.0",
  "description": "Enhanced SmartLead MCP server with analytics and SSE support",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc -w & node --watch dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "axios": "^1.6.2",            // Version from the example package.json you shared
    "dotenv": "^16.4.7"           // Version from the example package.json you shared
  },
  "devDependencies": {
    "@types/node": "^20.12.7",    // Assuming your current version is fine
    "typescript": "^5.3.3",       // Your current version
    "@types/axios": "^0.14.0"     // Optional but recommended for Axios typings
  },
  "license": "MIT"
}
